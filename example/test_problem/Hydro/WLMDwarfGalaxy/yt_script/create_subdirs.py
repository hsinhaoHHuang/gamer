from pathlib import Path


Path('./bins').mkdir(parents=True, exist_ok=True)
Path('./pdfs').mkdir(parents=True, exist_ok=True)
Path('./profs').mkdir(parents=True, exist_ok=True)
Path('./tables').mkdir(parents=True, exist_ok=True)
Path('./imgs_a').mkdir(parents=True, exist_ok=True)
Path('./imgs_pd').mkdir(parents=True, exist_ok=True)
Path('./imgs_tv').mkdir(parents=True, exist_ok=True)
Path('./imgs_o').mkdir(parents=True, exist_ok=True)
Path('./imgs_oa').mkdir(parents=True, exist_ok=True)
Path('./imgs_tl').mkdir(parents=True, exist_ok=True)
